/*
 * Copyright (c) 2015 Big boss
 * All rights reserved.
 */
package com.synflow.hello.src;

network HelloWorld {
	import com.synflow.hello.src.WordToDisplay;
	import com.synflow.hello.src.DriverSegment;

	wordToDisplay = new WordToDisplay();

	driverSegment = new DriverSegment();
	driverSegment.reads(wordToDisplay.character);

	printer = new task {
		void loop() {
			print(driverSegment.seg.read());
		}
	};

}
